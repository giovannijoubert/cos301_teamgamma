import 'dart:async';

class RegisterAuthentication {
  Future<bool> register(int userId) async {
    /* var fetchedUser = await _api.getUserProfile(userId);

    var hasUser = fetchedUser != null;
    if(hasUser) {
      userController.add(fetchedUser);
    } 

    return hasUser; */
    return true;
  }
}